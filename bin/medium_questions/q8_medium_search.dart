// 8. Write a function to search for a word in a 2D grid.

findWord(List<List<String>> array, String word) {
  int wordIndex = 0;
  int n = array.length;
  for(int i = 0; i < n; i++){
   for(int j = 0; j < array[i].length; j++){

     }
    }
}

void main() {
  List<List<String>> twoDGrid = [
    ['a', 'w', 'o', 'b'],
    ['b', 'c', 'r', 'd'],
    ['d', 'g', 't', 'w'],
    ['l', 'm', 'z', 'x'],
  ];
  String word = 'word';
  findWord(twoDGrid, word);
}
